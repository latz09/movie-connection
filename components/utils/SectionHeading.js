const SectionHeading = ({ title }) => {
	return (
		<div className="text-center text-3xl border-b text-neon-blue p-3">
			{' '}
			<h1>{title}</h1>{' '}
		</div>
	);
};

export default SectionHeading;
